services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models
      - ./reports:/app/reports
    command: uvicorn boston_house_prices.api:app --host 0.0.0.0 --port 8000 --reload
